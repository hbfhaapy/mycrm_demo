.center-box
  .content-box
    %h2 忘记密码?
    = devise_error_messages!
    .cell
      = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
        .field
          = f.label :login
          = f.email_field :login, :class => "text-input"
        .actions
          = f.submit "发送重置密码申请", :class => "btn"
      = render "devise/shared/links"
